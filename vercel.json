{
  "buildCommand": "npx expo export --platform web && cp -r dist/client/. dist/server/",
  "outputDirectory": "dist/server",
  "functions": {
    "api/materialen.js": {
      "memory": 512,
      "maxDuration": 60
    }
  },
  "rewrites": [
    { "source": "/api/materialen", "destination": "/api/materialen" }
  ]
}
